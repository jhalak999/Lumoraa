# The Vite dev server proxies /api and /static to the backend (see vite.config.ts),
# so no VITE_API_URL is needed for local development. For a production build served
# separately from the API, set this and update src/lib/api-client.ts's baseURL.
VITE_API_URL=http://localhost:8000
